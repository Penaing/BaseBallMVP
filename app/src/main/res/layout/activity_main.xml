<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".Game.View.GameActivity">

    <data>
        <variable
            name="viewModel"
            type="com.example.android.bullsandcows.Game.scene.login.LoginContracter.Presenter"/>
        <variable name="activity" type="com.example.android.bullsandcows.Game.View.GameActivity" />
        <variable name="presenter" type="com.example.android.bullsandcows.Game.Presenter.GamePresenter" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:orientation="vertical">

        <EditText
            android:id="@+id/id_edt"
            android:layout_width="100dp"
            android:layout_height="45dp"/>
        <EditText
            android:text="@{viewModel.userId}"
            android:id="@+id/pass_edt"
            android:layout_width="100dp"
            android:layout_height="45dp"/>

        <Button
            android:onClick="@{viewmodel.onClickLogin}"
            android:text="로그인"
            android:layout_width="100dp"
            android:layout_height="45dp"/>

        <FrameLayout
            android:id="@+id/display_framelayout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3" />
        
        <FrameLayout
            android:id="@+id/input_framelayout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="7" />

    </LinearLayout>

</layout>